import React from 'react';

// import Aux from '../../hoc/Aux';
import './Toolbar.css';

const toolbar = ( props ) => (
  <header className="Toolbar">
    <div>MENU </div>
    <div>LOGO</div>
    <nav>
      ...
    </nav>
  </header>
);

export default toolbar; 